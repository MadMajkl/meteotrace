{
  "name": "meteotrace",
  "version": "0.0.1",
  "description": "Počasí na trase — v bodech cesty v čase, kdy tam skutečně dorazíš.",
  "private": true,
  "type": "module",
  "scripts": {
    "selftest": "npm run selftest:logic",
    "selftest:logic": "node --test test/selftest-logic.mjs test/selftest-proxy.mjs",
    "serve": "node test/serve.mjs",
    "docx": "node tools/docx.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
