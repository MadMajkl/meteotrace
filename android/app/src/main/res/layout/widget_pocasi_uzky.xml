<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget 4 × 1 (R29) — nízký a široký; druhá velikost, na kterou jde
  stohovat se systémovým počasím.

  ⚠️ Místo popisu počasí je tu VĚTA O DEŠTI: popis říká ikona, a na jeden
  řádek se vejde jen jedna věc — ta, podle které se člověk rozhoduje.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/w_pozadi"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingEnd="14dp">

        <TextView
            android:id="@+id/w_ikona"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:textSize="30sp" />

        <TextView
            android:id="@+id/w_teplota"
            style="@style/WidgetText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:fontFamily="sans-serif-light"
            android:includeFontPadding="false"
            android:textSize="38sp" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/w_misto"
                style="@style/WidgetText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:singleLine="true"
                android:textSize="15sp" />

            <!-- ⚠️ Dva řádky, ne jeden: „Příštích 12 hodin nic nespadne" se
                 na šířku 4 × 1 při větším písmu nevejde a useknutá věta
                 („nic nesp…") neřekne nic. Když se vejde, řádek zůstane
                 jeden — `maxLines` výšku nenafoukne. -->
            <TextView
                android:id="@+id/w_veta"
                style="@style/WidgetText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.9"
                android:ellipsize="end"
                android:maxLines="2"
                android:textSize="13sp" />
        </LinearLayout>

        <!-- 🚨 Max/min tu NENÍ schválně. Po zvětšení písma (23. 9. 2026) se
             věta o dešti usekávala uprostřed — a z „Příštích 12 hodin nic
             nesp…" se nedá nic vyčíst. Na 4 × 1 má přednost věta; max/min
             ukazuje 4 × 2 i 2 × 2. Proto `w_maxmin` chybí i ve `VARIANTY`. -->
        <TextView
            android:id="@+id/w_cas"
            style="@style/WidgetText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:alpha="0.75"
            android:singleLine="true"
            android:textSize="11sp" />
    </LinearLayout>
</FrameLayout>
