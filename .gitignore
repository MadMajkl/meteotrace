# Nástroje a IDE
.claude/
.idea/
.vscode/
.tmp-shot/
*.bak
*.bak2

# Android — generované výstupy a lokální konfigurace
android/.gradle/
android/.idea/
android/local.properties
android/.kotlin/
android/build/
android/app/build/

# ⚠️ Podpisový klíč a hesla NIKDY do gitu (vzor: Gulpka)
android/keystore.properties
*.jks
*.keystore

# Kopie webu v obalu — generuje ji `npm run android:sync`, zdroj pravdy je web/
android/app/src/main/assets/www/

# Node (jen nástroje, samotná appka běží bez build kroku)
node_modules/

# Vlastní mapové dlaždice — velké binární soubory, negenerují se z repa (viz R3)
*.pmtiles

# Stažené nástroje (pmtiles.exe má 59 MB) — každý si je stáhne skriptem
tools/bin/

# Systém
Thumbs.db
.DS_Store

